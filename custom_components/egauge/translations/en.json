{
  "config": {
    "step": {
      "user": {
        "description": "eGauge login details. Leave Username and Password blank if your eGauge is not configured to require authentication.",
        "data": {
          "egauge_url": "eGauge URL",
          "username": "Username",
          "password": "Password"
        }
      }
    },
    "error": {
      "auth": "Cannot connect to eGauge. Check configuration."
    },
    "abort": {
      "single_instance_allowed": "Only a single instance is allowed."
    }
  },
  "options": {
    "step": {
      "user": {
        "data": {
          "binary_sensor": "Binary sensor enabled",
          "sensor": "Sensor enabled",
          "switch": "Switch enabled"
        }
      }
    }
  }
}
